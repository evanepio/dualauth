server:
  port: 9000

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/auth/realms/realm-a
          jwk-set-uri: http://localhost:8080/auth/realms/realm-a/protocol/openid-connect/certs